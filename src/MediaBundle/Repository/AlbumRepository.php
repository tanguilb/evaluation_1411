<?php

namespace MediaBundle\Repository;

/**
 * AlbumRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class AlbumRepository extends \Doctrine\ORM\EntityRepository
{
    public function findCommentaireByAlbumId($id)
    {

        $req = $this->createQueryBuilder('a')
            ->select()
            ->join('MediaBundle\Entity\Commentaire', 'c', 'WITH', 'a.id = c.commentaire')
            ->where('c.commentaire = ?1')
            ->setParameter(1, $id)
            ->getQuery();

        return $req->getResult();
    }
}
