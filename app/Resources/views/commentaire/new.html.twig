{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentaire creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <a href="{{ path('album_index') }}"><input type="submit" value="Create" /></a>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('album_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
